spring:
  config:
    import: optional:application-secret.yml
  application:
    name: Fant.org
  mail:
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


springdoc:
  api-docs:
    path: /api-docs
